# RTL 重构清单

本清单是
`docs/superpowers/specs/2026-07-29-compare-branch-isa-design.md`
中比较、分支和最小中断接口设计的 RTL 实施部分。

Codex 负责修改汇编器、Tiny C 编译器、软件测试和 ISA/ABI 文档；项目维护者
负责完成以下 RTL 工作。

## 寄存器组

- [ ] `r0` 继续恒定读出零，并忽略软件写入。
- [ ] 允许普通指令写入 `r1-r15`。
- [ ] 保留 `r1`、`r2`、`r3`、`r15` 的专用功能；软件可写不代表它们可以
      被编译器分配为通用寄存器。
- [ ] 普通指令写 `r1` 时完整更新其软件可见值；中断逻辑仍使用 bit 0 作为
      中断使能，使用 bits 2:1 作为触发模式。
- [ ] 普通指令写 `r2` 时更新中断向量字节地址。
- [ ] 普通指令写 `r3` 时更新中断链接字节地址，使软件可以恢复已保存的
      嵌套中断链接地址。
- [ ] 普通指令写 `r15` 时，直接把写入结果作为下一条 PC；不要为 `r15`
      增加一份与 `prog_addr`/PC 分离的寄存器副本。
- [ ] 删除 `r1[7:3]` 对比较标志的映射。
- [ ] 明确同一时钟边沿的写入优先级：复位最高，其次是已接受中断产生的
      架构写入，最后是普通指令写入。中断入口必须优先控制 `r1[0]`、`r3`
      和 `r15`/PC。

## 比较指令

- [ ] 删除旧 CMP 更新全局标志的行为，并删除 `ugt/uge/sgt/sge/eq` 架构状态。
- [ ] 将 opcode `0x3` 译码为立即数比较。
- [ ] 将 opcode `0x4` 译码为寄存器比较。
- [ ] opcode `0x3` 使用 `imm16`、`rs2`、`rd` 和 `funct=condition`。
- [ ] 所有条件都先把 `imm16` 符号扩展为 32 位，包括无符号比较。
- [ ] opcode `0x4` 使用 `rs1`、`rs2`、`rd` 和 `funct=condition`，
      bits `31:20` 为保留字段。
- [ ] 实现条件码：`0 EQ`、`1 NE`、`2 SGE`、`3 SLT`、`4 SGT`、
      `5 SLE`、`6 UGE`、`7 ULT`、`8 UGT`、`9 ULE`。
- [ ] 通过普通寄存器写回路径向 `rd` 写入严格的 `32'd1` 或 `32'd0`。
- [ ] 比较指令不得修改隐藏比较状态或任何专用寄存器，除非 `rd` 明确指定
      了该专用寄存器。
- [ ] 条件码 `0xa-0xf` 保留。

## 分支指令

- [ ] 用 BZ 替换原 funct `0xb` 的 CMP 译码。
- [ ] 用 BNZ 替换原 funct `0xc` 的 BRC 译码。
- [ ] opcode `0x1` 的目标地址为
      `R[rs2] + zero_extend(imm16)`。
- [ ] opcode `0x2` 的目标地址为 `R[rs2] + R[rs1]`。
- [ ] 使用 `R[rd]` 作为被判断的值。
- [ ] BZ 仅在 `R[rd] == 0` 时选择目标地址。
- [ ] BNZ 仅在 `R[rd] != 0` 时选择目标地址。
- [ ] 分支不成立时选择 `PC + 4`。
- [ ] 把最终选择的地址保留为 `resolved_next_pc`，确保同一条分支完成时
      接受中断，写入 `r3` 的仍是正确的实际后继地址。
- [ ] 不增加非对齐目标异常；动态生成的非对齐目标行为保持未指定。

## 最小中断行为

- [ ] 只在现有的指令完成边界接受中断。
- [ ] 接受中断时写入 `r3 = resolved_next_pc`。
- [ ] 接受中断时写入 `PC = R[r2]`。
- [ ] 接受中断时只清除 `r1[0]`，保持 `r1` 的其他位不变。
- [ ] 按现有触发锁存时序清除已经接受的待处理中断请求。
- [ ] 删除 `irq_active` 以及任何等价的硬件嵌套策略。
- [ ] 删除把 `jmp r3` 识别为中断返回指令的逻辑。
- [ ] 删除自动保存或恢复比较状态的逻辑。
- [ ] 不自动重新使能中断。
- [ ] 中断使能关闭期间发生的边沿事件不排队。
- [ ] 对于电平触发模式，软件重新使能后，如果输入电平仍然有效，允许再次
      触发中断。

## RTL 定向验证

- [ ] 对寄存器/寄存器形式的全部十种条件分别验证相等、小于和大于输入。
- [ ] 验证立即数边界 `-32768`、`32767` 和 `-1`。
- [ ] 验证无符号比较把符号扩展后的 `-1` 解释为 `32'hffff_ffff`。
- [ ] 验证每条比较指令只向 `rd` 写入 `0` 或 `1`。
- [ ] 验证 BZ 和 BNZ 的成立与不成立路径。
- [ ] 验证立即数分支目标对 `16'h8000` 和 `16'hffff` 执行零扩展，而不是
      符号扩展。
- [ ] 验证寄存器分支目标为 `R[rs2] + R[rs1]`。
- [ ] 验证用 `r0` 作为基址时可以形成绝对立即数或绝对寄存器目标。
- [ ] 验证在已跳转或已成立分支之后接受中断时，`r3` 保存已解析的目标，
      而不是固定的 `PC + 4`。
- [ ] 验证中断入口清除使能位但不改变触发模式位。
- [ ] 验证 `jmp r3` 没有任何隐藏副作用。
- [ ] Codex 完成汇编器和编译器修改后，运行软件生成的 Tiny C CPU、UART
      和最小中断镜像。
